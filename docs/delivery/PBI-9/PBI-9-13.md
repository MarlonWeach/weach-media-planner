# Task 9-13: Rastreabilidade da distribuição de budget (IA vs fallback) e racional

## Status
✅ Done

## Contexto
O % por formato no plano (step 4) vem de `gerarDistribuicaoBudgetPorFormato` (`lib/ia/mediaPlanner.ts`), não do mix por canal (`gerarMixMidia`). O resultado inclui `origem` (`ia` | `fallback_pesos`) e, quando IA responde, `racional` — persistido em `mixSugerido.distribuicaoFormatos` no banco.

## Objetivo
Permitir auditar se a alocação foi gerada por OpenAI ou pelo fallback determinístico, e qual foi o racional declarado.

## Escopo proposto
- [x] Expor `distribuicaoFormatos.origem` e `distribuicaoFormatos.racional` na resposta de `POST /api/cotacao/criar` e no step 4 do wizard (bloco colapsável “Como o plano foi gerado (auditoria)” + campo `cotacao.auditoriaMotor` na API).
- [x] Persistir também origem do mix por canal (`ia` | `fallback_padrao`) em `gerarMixMidia` / `RespostaMediaPlanner` (`origemMix`), refletido em `auditoriaMotor` ao ler `mixSugerido`.
- [x] `GET /api/cotacao/[id]` e edição de rascunho: devolvem / reidratam `auditoriaMotor` e extraem `mix` de `mixSugerido.mix` quando o JSON for objeto.
- [x] Motor de pesos por formato × objetivo (`lib/ia/pesosFormatoObjetivo.ts`) + limites pós-IA (CTV, Lead Ad > Tráfego, CPC).
- [x] Mix de referência por objetivo e tetos CPC no fallback/prompt (`lib/ia/mediaPlanner.ts`).
- [x] E-mail/PDF: formatos legíveis (`lib/cotacao/labelsModeloMidia.ts`) e briefing sem campos duplicados.
- [x] Correções de preço CTV/vídeo/áudio/CRM (chaves banco vs motor) em `formulasProgramaticas.ts`.
- [x] Dashboard: filtros e tabela responsivos.
- [ ] Registrar em `wp_HistoricoIA` prompt/resposta da distribuição por formato — **follow-up** (fora do MVP).
- [x] Documentar consulta `mixSugerido->distribuicaoFormatos` — ver [`guia-auditoria-motor-mix.md`](./guia-auditoria-motor-mix.md).
- [ ] (Opcional) Painel debug de últimas cotações com origem + racional.

## Critérios de aceite
- [x] Operador vê na interface ou via API se a distribuição foi `ia` ou `fallback_pesos` (e origem do mix por canal).
- [x] Quando `origem=ia`, o texto de `racional` fica acessível sem abrir JSON bruto no banco (wizard step 4).
- [x] Testes unitários do motor (`pesosFormatoObjetivo`, `auditoriaMotorMix`, fallbacks) — validados manualmente pela equipa.

## Referência técnica
- Fallback formato: `pesosFormatoObjetivo` + `aplicarPesosENormalizar` / `aplicarLimitesEstrategicosFormatos`.
- Fallback canal: `MIX_REFERENCIA_POR_OBJETIVO` em `mediaPlanner.ts`.
- IA: `gpt-4o-mini`, `response_format: json_object`, validação de índices e soma 100%.

## Concluído em
2026-05-18
