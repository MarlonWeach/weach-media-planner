# Task 9-11: Dashboard — manager vê todas as cotações

## Status
✅ Done

## Descrição
**Auditar e corrigir** a listagem de `/dashboard` para garantir que usuários com papel **manager** (ou equivalente no enum `Role` do projeto) visualizem **todas** as cotações do sistema (ou o escopo corporativo definido), e não apenas as do próprio vendedor.

## Objetivo
Alinhar permissões de gestão à operação: manager precisa de visão consolidada para acompanhamento e governança.

## Critérios de Aceite
- [ ] Matriz de teste documentada: `VENDEDOR` vê apenas o permitido hoje; `MANAGER` (e `ADMIN` se existir) vê conjunto completo esperado.
- [ ] API/route que alimenta o dashboard aplica `where` coerente com o role (sem filtro `vendedorId` indevido para manager).
- [ ] Teste manual ou automatizado referenciado no fechamento da task.
- [ ] Se “todas” tiver limite (ex.: só ativas), documentar explicitamente no critério.

## Notas
- Cruzar com `podeAcessarCotacao` / middleware de sessão e com qualquer `findMany` em `app/dashboard` ou `app/api/.../cotacoes`.
