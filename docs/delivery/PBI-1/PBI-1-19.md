# Task 1-19: Tornar filtros do dashboard dinâmicos via dados reais e adicionar filtro por solicitante

## Status
✅ Done

## Descrição
Ajustar os filtros da listagem de cotações no dashboard para que o campo `Status` seja preenchido dinamicamente com base nos dados reais disponíveis ao usuário logado, além de incluir o novo filtro por `Solicitante`.

## Critérios de Aceite
- [x] O filtro de status não exibe opções sem dados reais.
- [x] O filtro de status é retornado dinamicamente pela API.
- [x] Novo filtro de solicitante disponível no dashboard.
- [x] Filtro de solicitante aplicado no backend e frontend.
- [x] Respeito às regras de visibilidade por perfil (admin/comercial).

## Arquivos Alterados
- `app/api/cotacao/list/route.ts`
- `components/dashboard/CotacaoList.tsx`
- `docs/delivery/PBI-1/tasks.md`

## Notas Técnicas
- A API agora retorna `filtros.status` via `groupBy` e `filtros.solicitantes` via `distinct`.
- A montagem dos filtros respeita a visibilidade do usuário e os demais filtros ativos, removendo apenas o próprio filtro que está sendo calculado.
